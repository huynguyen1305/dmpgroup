import { ThemeConfig } from "antd";

export const antdTheme: ThemeConfig = {
  components: {
    Typography: {
      fontFamily: `'Montserrat', sans-serif`,
      fontSize: 16,
      colorText: "#FFFFFF",
    },
  },
};
