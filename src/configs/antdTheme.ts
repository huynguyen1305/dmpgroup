import { ThemeConfig } from "antd";

export const antdTheme: ThemeConfig = {
  components: {
    Typography: {
      fontFamily: `'Inter', sans-serif`,
      fontSize: 16,
      colorText: "#FFFFFF",
    },
  },
};
